# CRU2026 - Stan projektu

## Opis projektu
CRU2026 to wewnętrzny system do zarządzania umowami organizacji - scentralizowany rejestr obejmujący docelowo tysiące umów, dokumentację przechowywaną poza bazą (docelowe miejsce niezdecydowane - na razie serwer bytomski, patrz architektura) oraz kalendarz końca umów z automatycznymi przypomnieniami dla osób odpowiedzialnych (czy umowa ma zostać zakończona, czy odnowiona). To wyłącznie własny, wewnętrzny system - **bez** raportowania do rządowego Centralnego Rejestru Umów (usunięte 2026-07-15, nie ma znaczenia dla tego projektu). Jedyny element związany z administracją państwową to wyszukiwarka REGON (GUS), używana tylko do pobrania danych kontrahenta.

## Skala
- ~100 użytkowników
- ~2000 nowych umów **rocznie** (nie miesięcznie - poprawione 2026-07-15): ok. 1200 kontraktów projektowych + ok. 800 podpisanych umów
- Dane mogą być poufne - mieszczą się w istniejącej klasyfikacji bezpieczeństwa SharePoint; to nie jest system rządowy, dane historyczne w pełni dostępne (fizyczny serwer w Bytomiu)
- Plany przyszłościowe (nie wcześniej niż za rok, poza obecnym zakresem): wdrożenie światowe poza Polską - będzie wymagało rozważenia migracji hostingu na AWS zamiast Abacus

## ⚠️ PIVOT ARCHITEKTONICZNY (2026-07-17) — czytaj przed sekcją poniżej
Kierunek z 2026-07-15 (własny Postgres w Abacus + replika 1:1 legacy w Next.js) został **porzucony**. Nowa strategia = **strangler / Database-as-API**: legacy CakePHP + jego MySQL zostają nietknięte w Bytomiu (`10.222.125.213`), a my dokładamy JEDNĄ nową funkcję — **masowe podpisywanie umów przez Adobe Sign** na module `/cru/index.php/project`. Nowy stack: React SPA (Vite) + Node/Express, dev na VirtualBox/Debian → **prod na wewnętrznym serwerze w Katowicach** (sieć firmowa AM, Docker), Nginx+SSL(Let's Encrypt)+pm2. Baza+PDF w Bytomiu po **sieci wewnętrznej (LAN), bez VPN** (VPN tylko gdyby padło na zewnętrzny VPS). Publicznie wystawiony tylko **jeden endpoint HTTPS** (osobna domena, np. `podpisy.firma.com`) — wyłącznie dla Adobe (strona podpisu + webhook). Backend read-write wprost do tabel legacy. Backend pisze read-write wprost do tabel legacy. Sekcja „Zdecydowana architektura (2026-07-15)" poniżej jest **historyczna** — nieaktualna od pivotu. Szczegóły i otwarte kwestie: log 2026-07-17.

## Zdecydowana architektura (2026-07-15) — HISTORYCZNE, patrz pivot wyżej
- **Frontend/backend:** Next.js 14 (App Router) + TypeScript - ten sam stack co AMSteel_Quote / SafetyHub
- **Baza danych:** PostgreSQL, hostowana w chmurze przez Abacus (nie lokalnie). Metadane umów są małe objętościowo (rzędu pojedynczych GB nawet po latach), ale przy ~100 realnych użytkownikach ważniejsze są automatyczne backupy i uptime niż oszczędność na hostingu
- **Dlaczego hosting zewnętrzny (Abacus), a nie własny serwer po adresie IP (jak legacy `index.php`):** rozważaliśmy trzymanie appki na wewnętrznym serwerze, dostępnym po IP w sieci firmowej - tak jak dziś działa stara strona. Odrzucone, bo to dokładnie ten sam model, który doprowadził do sytuacji z legacy systemem: nikt wewnątrz firmy nie ma dziś dostępu do jego kodu ani go nie utrzymuje. Managed cloud (Abacus) daje automatyczne backupy i uptime bez trzymania osoby odpowiedzialnej za operacje serwera wewnątrz firmy. Jeśli potrzeba ograniczyć dostęp tylko do sieci ArcelorMittal, można to zrobić firewallem/VPN na Abacus - nie trzeba rezygnować z managed hostingu, żeby appka była widoczna tylko wewnętrznie. Dodatkowo appka i tak wymaga wyjścia na internet do Microsoft Graph (SharePoint, Entra ID, wysyłka maili), więc pełne odcięcie od świata przez czysty serwer wewnętrzny i tak nie eliminuje tej zależności
- **Pliki umów (skany/PDF) - niezdecydowane (zmiana 2026-07-15):** SharePoint miał być docelowym miejscem, ale użytkownicy zgłaszają, że jest kapryśny i wolny; firma równolegle rozmawia z AWS, które może stać się docelową infrastrukturą (termin nieznany). Obecnie pliki (nie tylko historyczne, także bieżące) leżą głównie na fizycznym serwerze w Bytomiu. Na tę chwilę aplikacja zakłada połączenie z serwerem bytomskim jako źródłem plików; techniczne szczegóły tej integracji (i ewentualną późniejszą migrację do SharePoint/AWS) zostawiamy do rozwiązania po stronie Abacus. Nie zmienia to zasady, że Postgres trzyma wyłącznie metadane, nigdy pliki
- **Logowanie (skorygowane 2026-07-16):** na tę chwilę **prosty natywny panel logowania (login + hasło)**, wzorowany na legacy CRU - **BEZ** Entra ID SSO. Wcześniejsze założenie, że logowanie legacy idzie przez Microsoft/Entra ID SSO, okazało się błędne: audyt Fazy 0 pokazał, że legacy CRU ma własny natywny formularz login+hasło (`index.php/users/login`, `forgot_password`), własną tabelę użytkowników, żadnego redirectu do Microsoft. Entra ID SSO pozostaje możliwym kierunkiem na przyszłość, ale **nie jest obecnym założeniem** i nie blokuje Fazy 0. Konsekwencja: nie zakładamy już, że app registration „przy okazji" pokrywa logowanie
- Katalog firmowy (Entra ID) służy też do wyboru osoby odpowiedzialnej za decyzję o zakończeniu/odnowieniu danej umowy - przypomnienia trafiają na aktualny, weryfikowalny adres
- **Przypomnienia o końcu umów** (dawniej: gwarancje - zmienione 2026-07-15, to nie jest osobny okres gwarancyjny, tylko data zakończenia samej umowy): zadanie cykliczne skanujące zbliżające się daty zakończenia, wysyłka **tylko e-mail** (bez Teams) przez Microsoft Graph API (`sendMail`) z dedykowanego adresu `mateusz.mazur@arcelormittal.com` z pytaniem czy umowa ma być zakończona czy odnowiona, + widok w aplikacji (kalendarz / lista nadchodzących końców umów)
- **Lista umów vs. podgląd pliku:** widok listy/tabeli czyta wyłącznie z Postgres (metadane) - nigdy nie odpytuje SharePoint przy renderowaniu wiersza. Plik z SharePoint pobierany jest leniwie, dopiero na żądanie (np. po kliknięciu ikony podglądu), więc skala tysięcy rekordów nie wpływa na płynność listy
- **Wyszukiwarka REGON:** integracja z GUS BIR1 API (`wyszukiwarkaregon.stat.gov.pl`) - po wpisaniu REGON kontrahenta, automatyczne uzupełnienie danych zamiast ręcznego wpisywania (Faza 2)
- **Branding:** wymóg klienta - obowiązkowe użycie firmowego logo, fontów i palety kolorów ArcelorMittal w UI, to nie jest opcjonalna kosmetyka. Assets dostarczone i posegregowane w `historia_wersji/Branding/` (logo SVG/PNG, ikony SVG, fonty Poppins + Gilroy, grafiki gradientowe). Paleta kolorów zmierzona z pikseli assetów (nie oficjalny brand guide): fiolet `#7F1878`, magenta `#C12974`, czerwień `#E74341`, pomarańcz `#F58847` - pełne dane w `historia_wersji/Branding/paleta_kolorow.md`. Priorytet UI: identyczny szkielet funkcjonalny co legacy strona + maksymalna czytelność, bez efekciarstwa wizualnego

## Zależności / do ustalenia z IT
- Jedno zgłoszenie do IT (szacowane ~15 min) obejmujące rejestrację aplikacji (app registration) w Entra ID z zakresami: `Files.ReadWrite` (SharePoint) + logowanie/SSO (`User.Read` / sign-in)
- Potwierdzenie, czy dane mogą być przetwarzane w chmurze (Abacus/AWS) bez ograniczeń rezydencji danych - wstępnie potwierdzone: tak, bo SharePoint już traktuje te dane jako poufne
- Exchange Application Access Policy - ograniczenie appki do wysyłki maili tylko z `mateusz.mazur@arcelormittal.com` (domyślnie app-only `Mail.Send` pozwala wysyłać jako dowolna skrzynka w tenantcie)
- Klucz API do usługi REGON (GUS BIR1) - kto występuje o rejestrację
- Wytyczne brandowe ArcelorMittal (logo, fonty, paleta kolorów) - potrzebne do finalizacji UI

## Struktura dokumentacji projektu
- `status_projektu.md` - ten plik, stan bieżący (aktualizować na bieżąco)
- `plan.md` - plan wdrożenia w fazach
- `historia_wersji/Historia wersji/` - raporty wersji (`CRU2026_Raport_Wersji_X.X.docx`)
- `historia_wersji/Opis aplikacji/` - opis aplikacji dla zarządu (`OPIS_APLIKACJI_CRU2026.docx`)
- `historia_wersji/Szablony/` - szablony do wykorzystania

## Log sesji

### 2026-07-17
- **PIVOT strategii (potwierdzony przez Mati).** Porzucamy greenfield rewrite (Abacus/Postgres + replika 1:1 Next.js). Nowy kierunek = **Database-as-API / strangler**: legacy CakePHP i jego MySQL zostają nietknięte w Bytomiu, dokładamy tylko funkcję **masowego podpisywania umów przez Adobe Sign** na module projektów (`/cru/index.php/project`).
- **Decyzje kierunkowe (4/4):** (1) pivot zastępuje stary plan; (2) backend **read-write wprost do tabel legacy** (Mati świadomie wybrał, mimo ryzyka rozjechania logiki CakePHP — patrz zabezpieczenia niżej); (3) połączenie VPS↔Bytom przez **WireGuard/VPN** (bez wystawiania 3306 do internetu); (4) **osobna domena** tylko dla podpisów, legacy zostaje wewnętrznie — VPS NIE jest reverse-proxy dla całego ruchu legacy.
- **Nowy stack:** React SPA (Vite) + Node/Express (`mysql2/promise`), Nginx reverse-proxy + Let's Encrypt SSL, pm2, `.env` na wszystkie sekrety (DB legacy, ścieżki PDF, Adobe Sign API). Frontend React (nie Vue) — spójność z resztą stacku Mati.
- **Zabezpieczenia dla read-write do legacy (do egzekwowania w implementacji):** (a) NAJPIERW zrzut schematu legacy MySQL + obserwacja, co CakePHP robi przy zapisie (timestampy `created/modified`, pola audytu „modyfikowano przez"=login, tabele HABTM dla relacji wiele-do-wielu, generowanie `record_id`, enumy statusów); (b) wszystkie zapisy w transakcjach; (c) pola audytu ustawiamy ręcznie; (d) start od NAJWĘŻSZEGO zapisu (zmiana statusu w flow Adobe Sign) + własna tabela na stan kopert Adobe, zamiast rozlewać się po tabelach legacy; (e) jeśli się da — test na kopii/stagingu bazy przed produkcją.
- **Blocker #1 (nierozwiązany):** dostęp do serwera bytomskiego. Audyt mówił wprost „lifetime support, brak dostępu do repo/serwera", `/admin` = panel vendora „W.B. Projekt". Nowy plan wymaga od tego serwera: połączenia do MySQL z zewnątrz, write do tabel, **write do katalogu PDF** (`/cru/media/…`), postawienia tunelu VPN. Wymaga zgody właściciela serwera i prawdopodobnie vendora — do potwierdzenia, czy dogadane.
- **Otwarte kwestie nowego kierunku (do dopytania):** (i) konto/licencja **Adobe Acrobat Sign** + dostęp API (integration key/OAuth), model bulk (jedna koperta na wiele umów vs mega-sign); (ii) **auth nowego SPA** — jak nowa appka wie, kto jest zalogowany i czyj login wpisać w audyt: czytać tabelę `users` legacy (jaki hashing haseł w CakePHP?) czy osobny login; (iii) mechanizm **zapisu podpisanego PDF** z webhooka Adobe do storage w Bytomiu (mount przez VPN / SFTP / nowa wersja pliku vs nadpisanie — legacy ma „Wersja ostateczna"); (iv) **compliance** — wystawienie dziś-wewnętrznych, poufnych danych umów przez publiczny VPS wymaga akceptacji bezpieczeństwa firmy; (v) schemat modułu `/project` nieznany od środka (audyt: 0 rekordów z konta Mati) — rozwiąże się zrzutem schematu MySQL.
- **Rozstrzygnięcia kwestii (i)–(iii) (ta sama sesja, później):**
  - **(i) Adobe Sign flow:** umowa idzie w chmurze do podpisu przez **kolejne ważne osoby** (multi-signer, podpis prawnie honorowany), **bez pobierania pliku** na komputer podpisującego — podpis na stronie w przeglądarce. Czyli klasyczne „send for signature" z wieloma odbiorcami + widget podpisu w iframe.
  - **(ii) Auth:** logowanie **weryfikowane o istniejącą tabelę `users` legacy** (te same konta/dane co dziś, bez równoległej bazy tożsamości). Funkcja **dodawania nowych kont** może żyć na nowym serwerze (UI). ⚠️ do potwierdzenia ze schematu/vendora: **jaki hashing haseł** stosuje to CakePHP — musimy go znać, żeby (a) weryfikować logowanie i (b) tworzyć nowe konta zgodne z loginem legacy.
  - **(iii) Zapis PDF:** podpisany plik z Adobe **nadpisuje oryginał** w storage bytomskim. ⚠️ zabezpieczenie: trzymać kopię przed-podpisową oryginału do czasu zakończenia podpisu, nadpisanie atomowe.
- **Blocker #1 (dostęp do serwera) — w toku:** Mati pisze maila do **Łukasza**, który dalej kontraktuje się z adminami serwera bytomskiego. Mail obejmuje listę potrzeb (VPN/WireGuard, konto MySQL R/W przez tunel, write do katalogu PDF, zrzut schematu, potwierdzenie braku kolizji z legacy/vendorem) **oraz sekcję bezpieczeństwa/compliance** (poufne dane dziś tylko wewnętrzne → udostępnienie strony podpisu przez internet wymaga akceptacji security; mitygacje: VPN zamiast publicznego 3306, HTTPS/Let's Encrypt, auth o konta legacy, brak trwałego składowania danych na VPS, sekrety w `.env`; RODO/rezydencja danych po stronie chmury Adobe). Draft maila przygotowany 2026-07-17.
- `plan.md` do przepisania pod nowy kierunek **dopiero po** domknięciu kwestii (i)–(iii) [zrobione] i potwierdzeniu dostępu do serwera (blocker #1).
- **Zatwierdzona architektura hostingu + Adobe (ta sama sesja, później):**
  - **Hosting:** dev na **VirtualBox/Debian** u Mati → prod na **wewnętrznym serwerze w Katowicach** (sieć firmowa AM), spakowane w **Docker**. NIE na prywatnym komputerze Mati — to antipattern, który stworzył legacy (serwer, którego nikt nie utrzymuje). Docker = identyczne środowisko dev↔prod, przeniesienie jednym poleceniem.
  - **Sieć (korekta względem banera/wcześniejszej decyzji o VPN):** serwer w Katowicach dobija do bazy+PDF w Bytomiu **po LAN firmowym, bez VPN i bez wystawiania bazy do internetu**. VPN zostaje w grze tylko dla scenariusza zewnętrznego VPS. Publicznie wystawiony **jeden endpoint HTTPS** (osobna domena) — wyłącznie dla Adobe (strona podpisu + webhook oddający podpisany plik).
  - **Adobe Sign — podejście:** hosted link Adobe (podpisujący klika mail → podpisuje w przeglądarce Adobe, zero instalacji, zero pliku u niego), odbiorcy w kolejności, **webhook** oddaje podpisany PDF. Embedded signing = polish na później, nie na start. „Bulk" = N osobnych agreements (każda umowa = własny podpisany PDF), NIE Adobe Mega Sign. Wygoda po naszej stronie: bulk checkbox-select + preset odbiorców + dashboard statusów. Sandbox Adobe przed produkcją.
  - **Prereki Adobe:** konto Acrobat Sign z dostępem API (business/enterprise, OAuth S2S) — **płatny prereq, do potwierdzenia z firmą**; poziom podpisu (zwykły eIDAS vs kwalifikowany QES) — do potwierdzenia z działem prawnym.
- **Maile wysyłane 2026-07-17:** (1) do **Łukasza** (koordynuje z adminami + pyta o akceptację security i licencję Adobe API), (2) załącznik dla admina serwera (dostęp DB + ścieżka PDF + `mysqldump --no-data`). Poprawka względem wcześniejszego draftu: framing „VPN" → „serwer wewnętrzny w Katowicach po LAN" (spójne z zatwierdzoną architekturą); prośba o schemat = `mysqldump`, nie „zrzut ekranu".
- **`plan.md` przepisany 2026-07-17** pod nowy kierunek (Faza 0 dostęp/prereki → 1 szkielet+połączenie → 2 wysyłka do podpisu → 3 powrót podpisu+nadpisanie → 4 prod/utwardzenie). Master-prompty Fazy 0 oznaczone jako nieaktualne.

### 2026-07-16
- **Start Fazy 0a (audyt legacy CRU).** Legacy dostępne pod `http://10.222.125.213/cru/index.php/contract` (sieć wewnętrzna). Framework wygląda na CakePHP (wzorzec URL `index.php/controller/action`). Tytuł karty: „AMDS Quotation System", nagłówek appki: „Centralny Rejestr Umów".
- **Korekta założenia o logowaniu:** legacy NIE używa Entra ID SSO - ma własny natywny formularz login+hasło (`/users/login`, `/forgot_password`) i własną tabelę użytkowników. Decyzja Mati: na tę chwilę replika zostaje przy prostym natywnym panelu logowania, SSO to błędne założenie na teraz (patrz sekcja Logowanie wyżej). Odnotowano też osobną ścieżkę `/admin/` (oddzielny panel administracyjny - do zbadania w audycie).
- **Zakres legacy szerszy niż zakładał plan:** menu główne ma 10 pozycji - Umowy, Projekty, Dział ryzyka, Supply chain, Kontrahenci, Grupy, Lokalizacja dostępy, Dostępy, Raporty, Mailing. To znacznie więcej niż „rejestr umów + załączniki" z pierwotnego planu. Pełny inwentarz powstaje w `historia_wersji/audyt_legacy_strony.md`.
- Ustalona architektura: Abacus (Postgres) + SharePoint (pliki) + Entra ID (SSO)
- Utworzona struktura projektu i dokumentacja startowa (ten plik, `plan.md`, raport wersji 1.0, opis aplikacji)
- Doprecyzowano: lista umów zawsze czyta z Postgres, nie z SharePoint, więc nie ma ryzyka lagu przy przewijaniu tysięcy pozycji; pytanie otwarte - czy metadane (data, wartość, status) mają być wpisywane ręcznie, czy wyciągane automatycznie z treści PDF
- Kod źródłowy jeszcze nie istnieje - projekt na etapie planowania
- **Zmiana planu:** dodana nowa Faza 0 - istnieje działająca legacy strona (`index.php`, sieć wewnętrzna, np. `10.222.x.x/...`), do której nie mamy dostępu do kodu źródłowego (lifetime support, brak dostępu do repo/serwera), tylko do działającej aplikacji przez przeglądarkę. Zanim ruszy reszta planu (dawna Faza 0 → teraz Faza 1), robimy: (0a) pełny audyt/walkthrough starej strony ze screenshotami i spisem funkcji, (0b) osobny dokument z zakresem repliki 1:1 do akceptacji, (0c) implementacja repliki w Next.js jako punkt wyjścia. Reszta faz planu przesunięta o +1 (patrz `plan.md`)
- **Nowa Faza 7 (dodana):** asystent AI / Copilot - „latająca" ikonka na stronie do streszczania/tłumaczenia umów, oparta o Microsoft Copilot Studio (agent z SharePoint jako źródłem wiedzy, embed jako web chat widget w Next.js). Kosmetyczna, najniższy priorytet, na samym końcu planu - nie blokuje żadnej innej fazy. Zablokowana do czasu potwierdzenia z IT, czy istniejąca licencja M365 Copilot (per-seat) pokrywa też Copilot Studio (osobny model licencjonowania: capacity/consumption), czy trzeba dokupić osobno
- **Korekty (ta sesja):** skala poprawiona z ~2000 umów/miesiąc na ~2000/rok (1200 projektowych + 800 podpisanych); przypomnienia uproszczone do samego e-maila (bez Teams), wysyłane przez Graph API z `mateusz.mazur@arcelormittal.com`; dodana integracja z wyszukiwarką REGON (GUS BIR1) w Fazie 2, żeby nie wpisywać danych kontrahenta ręcznie; dodany wymóg brandingu ArcelorMittal (logo/fonty/kolory) jako obowiązkowy, nie kosmetyczny; odnotowane plany światowego wdrożenia + ew. migracji na AWS jako temat na przyszły rok, poza obecnym zakresem
- **Zmiana koncepcji (ta sesja):** wycofano osobny model „OkresGwarancji" (WarrantyPeriod) - Faza 3 dotyczy teraz wprost daty zakończenia samej umowy: przypomnienie i decyzja „zakończyć czy odnowić", bez osobnej encji gwarancyjnej
- **Usunięto raportowanie do CRU (ta sesja):** to wyłącznie własny, wewnętrzny system - dawna Faza 5 (Eksport do CRU) skasowana z planu; Utwardzenie i Copilot przesunięte o -1 (teraz Faza 5 i 6). Jedyny rządowy element to wyszukiwarka REGON (Faza 2), zostaje bez zmian
- **Potwierdzono hosting zewnętrzny (Abacus) zamiast własnego serwera po IP:** rozważono alternatywę - hostować appkę jak legacy `index.php` (serwer wewnętrzny, dostęp po adresie IP w sieci firmowej), bez zewnętrznego hostingu. Odrzucone: to ten sam model, który doprowadził do tego, że dziś nikt nie ma dostępu do kodu legacy systemu ani go nie utrzymuje. Zostajemy przy Abacus (managed backupy/uptime); jeśli trzeba ograniczyć appkę tylko do sieci firmowej, robimy to firewallem/VPN na Abacus, nie rezygnując z managed hostingu
- **Zaktualizowano `OPIS_APLIKACJI_CRU2026.docx`** (dokument dla zarządu, `historia_wersji/Opis aplikacji/`) - był nieaktualny względem wszystkich korekt z tej sesji. Poprawiono: skalę (rocznie, nie miesięcznie), zastąpiono koncepcję gwarancji końcem umowy + decyzją odnowić/zakończyć, dodano REGON, usunięto kanał Teams (tylko e-mail), usunięto całą sekcję o raportowaniu do CRU, dodano wzmiankę o Fazie 0 (replika legacy) i brandingu ArcelorMittal
- **Dostarczone assety brandowe ArcelorMittal (ta sesja, później):** Poppins (TTF, zainstalowany jako font systemowy) + Gilroy (WOFF/WOFF2, web font) w `historia_wersji/Branding/fonts/`; logo (SVG+PNG), 3 ikony SVG (Calendar, Housing, Settings) i 2 grafiki gradientowe w `historia_wersji/Branding/{logo,icons,graphics}/`. Paleta kolorów zmierzona pikselowo z `AM_Graphic_Shapes_RGB_A4_4.png` (Python/Pillow) i zapisana w `historia_wersji/Branding/paleta_kolorow.md`: fiolet `#7F1878`, magenta `#C12974`, czerwień `#E74341`, pomarańcz `#F58847` - przybliżenie, nie oficjalny brand guide. Mati doprecyzował priorytet UI: nie ma być "niesamowite wizualnie", ważniejszy jest identyczny szkielet funkcjonalny co legacy PHP + maksymalna czytelność
- **Zmiana: docelowe miejsce przechowywania plików niezdecydowane (ta sesja, później):** SharePoint miał być stałym rozwiązaniem, ale użytkownicy zgłaszają, że jest kapryśny i wolny; firma prowadzi rozmowy z AWS, który może zostać docelową infrastrukturą (termin nieznany). Obecnie pliki (bieżące, nie tylko historyczne) leżą głównie na serwerze w Bytomiu. Na razie zakładamy połączenie aplikacji z serwerem bytomskim jako źródłem plików, a techniczne rozwiązanie tej integracji zostawiamy Abacusowi (hosting/dev partner) - nie blokuje to rejestru/metadanych w Postgresie, które zostają bez zmian. Zaktualizowano `plan.md` (Stack techniczny, Model danych, Faza 2, Otwarte pytania)

## Następne kroki / otwarte tematy
- [ ] **Faza 0 (nowa, blokująca):** audyt legacy strony (index.php) - Mati przechodzi przez ekrany/funkcje, spisujemy wszystko, potem osobny dokument zakresu do akceptacji, potem replika w Next.js
- [ ] Zgłoszenie do IT: app registration w Entra ID (Graph API + SSO scopes)
- [ ] Provisioning bazy Postgres w Abacus
- [ ] Scaffold repo Next.js (wzorem AMSteel_Quote / SafetyHub)
- [ ] Doprecyzowanie typów umów i pól rejestru z zespołem
- [ ] Ustalenie: metadane wpisywane ręcznie przy dodaniu umowy, czy automatycznie wyciągane z treści PDF (OCR/document AI) - różne nakłady pracy
- [ ] Plan migracji istniejących umów (Excel/skany) do rejestru
- [ ] Podgląd dokumentu w aplikacji (ikona „oko") - Faza 2, patrz plan.md
- [ ] Potwierdzenie z IT: czy licencja M365 Copilot pokrywa Copilot Studio (blokuje Fazę 6, kosmetyczna/niski priorytet)
- [ ] Rejestracja klucza API do usługi REGON (GUS BIR1) - blokuje integrację w Fazie 2
- [ ] Exchange Application Access Policy ograniczająca wysyłkę maili do jednej skrzynki (`mateusz.mazur@arcelormittal.com`) - do zgłoszenia razem z app registration w Fazie 1
- [ ] Potwierdzić: wysyłka z osobistego adresu Mati czy z dedykowanej skrzynki współdzielonej - wpływa na utrzymanie w przyszłości
- [x] Pozyskanie wytycznych brandowych ArcelorMittal (logo, fonty, kolory) - dostarczone 2026-07-15, patrz `historia_wersji/Branding/`. Paleta kolorów to przybliżenie zmierzone z assetów, podmienić jeśli pojawi się oficjalny brand guide z dokładnymi hex/CMYK
- [ ] Docelowe miejsce przechowywania plików (SharePoint vs. serwer bytomski vs. AWS) - czeka na rozstrzygnięcie firmowych rozmów z AWS; na razie zakładamy serwer bytomski, integrację robi Abacus
